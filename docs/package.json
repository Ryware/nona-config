{
  "name": "docs",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "npm run generate:cli && astro build",
    "archive": "rm -rf artifacts && mkdir -p artifacts && cd dist && zip -qr ../artifacts/docs-html.zip .",
    "ci": "npm run build && npm run archive",
    "preview": "astro preview",
    "generate:cli": "node scripts/generate-cli-reference.mjs",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@astrojs/starlight": "^0.41.1",
    "astro": "^7.1.3",
    "sharp": "^0.34.5"
  }
}
